import { ProjectTemplate } from '../types';

export const reactRouterTemplates: ProjectTemplate[] = [
  {
    name: 'default',
    description: 'Create a new React Router project with the default template',
    command: 'npx create-react-router@latest PROJECT_NAME',
  },
]; 